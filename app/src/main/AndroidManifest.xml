<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bluetoothchat">

    <!--declara um recurso hard/soft utilizado pelo app informando do que ele depende nesse caso bluetooth-->
    <uses-feature android:name="android.hardware.bluetooth"/>
    <!--permite conectar a dispositivos bluetooth pareados-->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <!--permite descobrir e parear dispositivos bluetooth-->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <!--permite acesso a uma localizacao aproximada-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!--permite acesso a uma localizacao precisa-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>